Place one-time Postgres bootstrap scripts here when an environment needs them.

Application schema migrations live in the repository-level `migrations/` folder
and are executed by `alpha-studio-backend migrate` or automatically on API boot.
