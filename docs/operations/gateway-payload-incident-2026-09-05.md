# 2026-09-05 报告检查阶段 HTTP 413

## 原因与证据

请求 ID：`1dca2569-22bd-4357-8351-31e02f7d1e2d`。

本机会话 `01a07208-0d9d-7ed2-b580-79213ed071df` 和生产 API 只读日志确认：

- 北京时间 23:08:55，Chrome 打印命令已成功生成 10 页 PDF，文件大小 1,954,411 字节。
- 23:09:20，模型读取排版检查用的联系表图片；这次工具结果的序列化大小约 673 KB。
- 累积记录的 response items 以紧凑 UTF-8 JSON 序列化合计约 2.23 MB。此值用于说明上下文体量，并非抓包测得的完整 HTTP 请求大小。
- 23:09:24，生产 API 日志中相同请求 ID 的 `POST /v1/responses` 返回 413，耗时约 1.52 秒；客户端收到 `Failed to buffer the request body: length limit exceeded`。

模型接口使用 Axum 的 JSON extractor，却没有单独设置请求体上限，因此沿用默认 2 MiB。历史与图片累积超过阈值后，在上游推理之前被拒绝。PDF 本地打印已经完成；失败的是后续模型检查和最终回复。它是可复现的容量配置缺陷，与 PDF 页数本身无关。

## 修复与验证

- 仅将 `/v1/responses` 上限设为 20 MiB，低于现有 Caddy 的 25 MB 限制。普通 API 和 Skill 上传限制保持原有设置。
- 超限返回可识别的 JSON 错误代码与字节上限；其他 JSON 错误保持原 HTTP 状态。
- 客户端显示中文说明，保留原始诊断和已有工具结果，提示已生成文件仍可打开或打印。
- 用中文报告历史加预览图的合成请求验证：修改前得到 413，修改后进入鉴权。测试不需要真实模型请求或推理费用。
- 回归覆盖 20 MiB 边界、超出 1 字节、流式请求、普通接口仍受限、无效 JSON、错误提示和文件工具记录保留。

## 生效条件

本次已修改源码并完成本地验证，生产环境仅做只读日志查询，尚未部署。请求容量修复需部署 API；中文提示需发布桌面客户端。已有 PDF 可以直接打印，无需重新生成报告。
